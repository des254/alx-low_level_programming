#include "main.h"
/**
 * positive_or_negative - print positive or negative numbers
 *
 *@i: Number to be printed
 *
 * Return: 0
 */
void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
