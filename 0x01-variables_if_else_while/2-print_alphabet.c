#include <stdio.h>
/**
 * main - A program that prints alphabet in lowercase
 * Return: o (Success)
 */
int main(void)
{
	char c = 'a';

	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
